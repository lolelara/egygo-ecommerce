{
  "name": "vendoor-scraper-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for scraping Vendoor products using Puppeteer",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "@cloudflare/puppeteer": "^0.0.8"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
