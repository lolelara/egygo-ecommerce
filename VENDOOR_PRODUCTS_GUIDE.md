# 🛍️ دليل إدارة منتجات Vendoor

## 📋 المحتويات

1. [نظرة عامة](#نظرة-عامة)
2. [الإعداد الأولي](#الإعداد-الأولي)
3. [استخدام Admin Panel](#استخدام-admin-panel)
4. [المميزات](#المميزات)
5. [API Documentation](#api-documentation)

---

## 🎯 نظرة عامة

نظام متكامل لإدارة المنتجات المستوردة من Vendoor بسهولة:

### ✅ المميزات الرئيسية:

- **تصنيف تلقائي**: جميع المنتجات التي SKU الخاص بها يبدأ بـ "VN" تُصنف كمنتجات Vendoor
- **هامش ربح مرن**: إضافة نسبة مئوية (%) أو مبلغ ثابت (جنيه)
- **تطبيق جماعي**: تحديث أسعار جميع منتجات Vendoor بضغطة زر واحدة
- **تحكم فردي**: إمكانية تعديل كل منتج على حدة
- **حالات المنتجات**: إظهار/إخفاء المنتجات (draft/published)
- **إدارة مجمّعة**: اختيار عدة منتجات وتطبيق إجراءات عليها دفعة واحدة

---

## ⚙️ الإعداد الأولي

### 1️⃣ إضافة الـ Collections والـ Attributes

```bash
# في مجلد المشروع
cd c:\Users\NoteBook\Desktop\goegy-main

# تشغيل سكريبت الإعداد
node scripts/setup-vendoor-collections.mjs
```

**ماذا يفعل هذا السكريبت؟**
- ✅ ينشئ collection جديد: `vendoor_settings`
- ✅ يضيف attributes جديدة لـ `products`:
  - `originalPrice` (Float) - السعر الأصلي
  - `profitMargin` (Float) - هامش الربح
  - `profitType` (String) - نوع الهامش (percentage/fixed)

### 2️⃣ التحقق من Appwrite Console

افتح:
```
https://cloud.appwrite.io/console/project-{PROJECT_ID}/databases/{DATABASE_ID}
```

تأكد من وجود:
- ✅ Collection: `vendoor_settings`
- ✅ Attributes في `products`: `originalPrice`, `profitMargin`, `profitType`

---

## 💻 استخدام Admin Panel

### 1️⃣ الدخول إلى لوحة التحكم

```
الموقع → تسجيل الدخول (كـ Admin) → Admin Panel → Vendoor Products
```

**الرابط المباشر:**
```
http://localhost:5000/#/admin/vendoor-products
```

---

### 2️⃣ إعدادات هامش الربح

في صفحة Vendoor Products، ستجد قسم "إعدادات هامش الربح" في الأعلى.

**الخيارات المتاحة:**

1. **نوع الهامش**:
   - نسبة مئوية (%) - مثال: 5%
   - مبلغ ثابت (جنيه) - مثال: 10 جنيه

2. **قيمة الهامش**: أدخل الرقم المطلوب

3. **الإجراءات**:
   - **حفظ الإعدادات**: لحفظ القيم للاستخدام لاحقاً
   - **تطبيق على الكل**: لتحديث جميع المنتجات فوراً

**مثال:**
```
منتج بسعر 100 جنيه + هامش 5% = 105 جنيه
منتج بسعر 100 جنيه + هامش 10 جنيه = 110 جنيه
```

---

## 🎯 المميزات

### ✅ ما تم تنفيذه:

1. **Admin Panel Page**: صفحة مخصصة لإدارة منتجات Vendoor
2. **API Routes**: 6 endpoints للتحكم الكامل
3. **Profit Margin System**: نظام هامش ربح مرن
4. **Bulk Actions**: إجراءات جماعية على المنتجات
5. **Status Management**: إدارة حالة المنتجات (draft/published)
6. **Real-time Preview**: معاينة فورية للأسعار

---

## 🚀 الخطوات التالية

### 1. شغل السكريبت على الـ VM:

```bash
# على Azure VM
cd ~/vendoor-scraper
node vendoor-complete-final-v2.mjs
```

### 2. تحديث الإعدادات:

```bash
# على جهازك المحلي
cd c:\Users\NoteBook\Desktop\goegy-main
node scripts/setup-vendoor-collections.mjs
```

### 3. الوصول للـ Admin Panel:

```
http://localhost:5000/#/admin/vendoor-products
```

---

## 📊 API Endpoints

### GET /api/vendoor-products
جلب منتجات Vendoor مع pagination

### GET /api/vendoor-settings  
جلب إعدادات هامش الربح

### PUT /api/vendoor-settings
تحديث الإعدادات

### POST /api/apply-profit-margin
تطبيق هامش الربح على جميع المنتجات

### POST /api/vendoor-products/bulk-status
تحديث حالة منتجات متعددة

---

## ✅ تم بنجاح!

**الملفات المُنشأة:**

1. ✅ `server/routes/vendoor-products.ts` - API Routes
2. ✅ `client/pages/admin/VendoorProducts.tsx` - Admin Page
3. ✅ `scripts/setup-vendoor-collections.mjs` - Setup Script
4. ✅ `VENDOOR_PRODUCTS_GUIDE.md` - هذا الدليل

**التحديثات:**

1. ✅ `server/index.ts` - تسجيل الـ routes
2. ✅ `client/App.tsx` - إضافة الـ route
3. ✅ `client/lib/lazy-routes.tsx` - Lazy loading

---

## 🎉 كل شيء جاهز!

الآن يمكنك:
- ✅ إدارة منتجات Vendoor بسهولة
- ✅ إضافة هامش ربح مرن
- ✅ نشر/إخفاء المنتجات
- ✅ تطبيق إجراءات جماعية

**استمتع بالنظام! 🚀**
